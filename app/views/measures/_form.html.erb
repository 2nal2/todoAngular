
<%= form_for(@measure, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.form_group :unit, class: 'row' do |f| %>
      <%= f.label :unit, class: 'control-label col-md-2' %>
      <div class='col-12 col-md-10'>
        <%= f.text_field :unit, class: 'form-control' %>
        <%= f.error_messages %>
      </div>
    <% end %>

  </div>

  <div class="d-flex">
    <div class="ml-auto">
      <%= f.submit class: 'btn btn-primary' %>
      <%= link_to "Cancel", measures_path, class: 'btn btn-outline-default' %>
    </div>
  </div>
<% end %>

