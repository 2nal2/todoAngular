<% @title = "Log in" %>
<div id="main" class="devise-1">
  <div class="container-fluid">
    <div class="row">
      <header class="text-center">
        <h1><%=app_name%></h1>
        <p class="p-0 m-0 text-uppercase">Login</p>
      </header>
    </div>
    <div class="row mt-5">
      <div class="col-12 col-md-6 mr-auto ml-auto">
        <%= render partial: "layouts/flashes" %>
        <div class="card">
          <div class="card-body">
            <div class="container">
              <%= form_for resource, as: resource_name, url: session_path(resource_name),
                html: { class: "form-horizontal" } do |f| %>
                <%- if devise_mapping.omniauthable? %>
                  <p>
                    <%- resource_class.omniauth_providers.each do |provider| %>
                      <%= link_to fa_icon_tag("#{provider}-sign") + " Sign in with #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider), class: "btn" %>
                    <% end -%>
                  </p>
                  <p>Or sign in using your ...</p>
                <% end -%>
                <%= f.form_group :email, class: "row" do |f| %>
                  <%= f.label :email, class: "form-label" %>
                  <%= f.email_field :email, autofocus: true, class: "form-control" %>
                  <%= f.error_messages %>
                <% end %>
                <%= f.form_group :password, class: "row" do |f| %>
                  <%= f.label :password, class: "form-label" %>
                  <%= f.password_field :password, class: "form-control" %>
                  <%= f.error_messages %>
                <% end %>
                <% if devise_mapping.rememberable? %>
                  <div class="d-flex align-items-center">
                    <div class="checkbox m-0 mr-auto">
                      <%= f.check_box :remember_me %>
                      <%= f.label :remember_me, class: "checkbox m-0 mr-auto" %>
                    </div>
                    <%= f.submit "Log in", class: "btn btn-primary" %>
                  </div>
                <% end %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-12 col-md-6 mr-md-auto ml-md-auto">
        <ul class="list-unstyled options row">
          <li class="col-12 col-md-6"><%= link_to "Registrarse", new_user_registration_path %></li>
          <%- if devise_mapping.recoverable? %>
            <li class="col-12 col-md-6"><%= link_to "Olvidaste tu contraseña?", new_user_password_path %></li>
          <% end -%>
          <%- if devise_mapping.confirmable? %>
            <li class="col-12 col-md-6"><%= link_to "No recibiste las instrucciones de confirmación?", new_user_confirmation_path %></li>
          <% end -%>
          <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) %>
            <li class="col-12 col-md-6"><%= link_to "No recibiste intrucciones para desbloquear?", new_user_unlock_path %></li>
          <% end -%>
        </ul>
      </div>
    </div>
  </div>
</div>
